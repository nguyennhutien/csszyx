{
  "name": "@csszyx/playground-vanilla-html",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "esbuild src/main.ts --bundle --outfile=csszyx.js --minify --platform=browser --format=iife --define:process.env.NODE_ENV='\"production\"'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "esbuild": "^0.20.0"
  }
}
