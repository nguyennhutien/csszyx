# Prose gate. CI runs this on the lines a pull request adds (reviewdog
# filter_mode: added); the rules and why they are the set they are live in
# .agent/rules/categories/prose-style.md and the 2026-08-29 measurement
# report. Errors block; warnings annotate.
StylesPath = .vale/styles
MinAlertLevel = warning

[formats]
mdx = md

[*.{md,mdx}]
BasedOnStyles = csszyx
