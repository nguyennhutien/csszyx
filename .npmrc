# pnpm 11 no longer reads pnpm behavior settings from .npmrc. The former entries
# (shamefully-hoist, strict-peer-dependencies, auto-install-peers) now live in
# pnpm-workspace.yaml as shamefullyHoist / strictPeerDependencies / autoInstallPeers.
# Keep this file as a breadcrumb; add only non-pnpm npmrc keys (e.g. registry) here.
