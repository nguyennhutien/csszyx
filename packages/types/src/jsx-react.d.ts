/**
 * React JSX augmentation — an explicit alias of `@csszyx/types/jsx`, provided for
 * symmetry with `@csszyx/types/jsx-solid` so the framework name is discoverable
 * either way. `@csszyx/types/jsx` and `@csszyx/types/jsx-react` are identical: both
 * add the `sz` prop to React's intrinsic JSX elements.
 *
 * @module @csszyx/types/jsx-react
 */

/// <reference path="./jsx.d.ts" />
