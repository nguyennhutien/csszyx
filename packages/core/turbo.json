{
    "$schema": "https://turborepo.com/schema.json",
    "extends": ["//"],
    "tasks": {
        "test": {
            "dependsOn": ["^build", "build"],
            "outputs": ["coverage/**"],
            "inputs": ["src/**", "tests/**", "test/**", "Cargo.toml", "*.config.js", "*.config.ts"]
        }
    }
}
