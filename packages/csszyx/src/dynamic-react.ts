/**
 * csszyx/dynamic/react — React hooks for @csszyx/dynamic.
 *
 * Re-exports @csszyx/dynamic/react for use via the umbrella package.
 *
 * @example
 * import { useSz, useDynamicScope } from 'csszyx/dynamic/react';
 */
export type { UseSzReturn } from '@csszyx/dynamic/react';
export { CsszyxProvider, sz, useSz } from '@csszyx/dynamic/react';
