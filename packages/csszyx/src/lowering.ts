/**
 * `csszyx/lowering` — side-effect mirror of `@csszyx/runtime/lowering`.
 *
 * Same-package companion to `csszyx/core` (see that module for why the
 * rewrite never crosses package boundaries): one bare import makes the core
 * helpers object-capable.
 */
import '@csszyx/runtime/lowering';
