# Load mise shims if they exist to ensure node/npx are available in GUI git clients (like Fork)
export PATH="$HOME/.cargo/bin:$HOME/.local/share/mise/shims:/opt/homebrew/bin:$PATH"

scripts/verify-commit-message.sh "$1"
