---
title: Effects & Filters
description: Box shadow, opacity, filter, backdrop-filter, and blend mode props.
---

import { PropTable, ShadowViz, OpacityViz } from '../../../../components/PropTable.tsx';
import { BoxShadowScale, OpacityScale, BlurScale, GrayscaleFilter } from '../../../../components/demos/effects.tsx';

## Box Shadow

<PropTable rows={[
  { sz: "{ shadow: 'sm' }", tw: "shadow-sm", viz: <ShadowViz shadow="sm" /> },
  { sz: "{ shadow: true }", tw: "shadow", viz: <ShadowViz shadow={true} /> },
  { sz: "{ shadow: 'md' }", tw: "shadow-md", viz: <ShadowViz shadow="md" /> },
  { sz: "{ shadow: 'lg' }", tw: "shadow-lg", viz: <ShadowViz shadow="lg" /> },
  { sz: "{ shadow: 'xl' }", tw: "shadow-xl", viz: <ShadowViz shadow="xl" /> },
  { sz: "{ shadow: '2xl' }", tw: "shadow-2xl", viz: <ShadowViz shadow="2xl" /> },
  { sz: "{ shadow: 'inner' }", tw: "shadow-inner", viz: <ShadowViz shadow="inner" /> },
  { sz: "{ shadow: 'none' }", tw: "shadow-none" },
  { sz: "{ shadowColor: 'blue-500' }", tw: "shadow-blue-500" },
  { sz: "{ shadowColor: { color: 'blue-500', op: 30 } }", tw: "shadow-blue-500/30" },
]} />

<BoxShadowScale />

## Opacity

<PropTable rows={[
  { sz: "{ opacity: 0 }", tw: "opacity-0", viz: <OpacityViz opacity={0} /> },
  { sz: "{ opacity: 50 }", tw: "opacity-50", viz: <OpacityViz opacity={50} /> },
  { sz: "{ opacity: 75 }", tw: "opacity-75", viz: <OpacityViz opacity={75} /> },
  { sz: "{ opacity: 100 }", tw: "opacity-100", viz: <OpacityViz opacity={100} /> },
  { sz: "{ opacity: 35 }", tw: "opacity-35", viz: <OpacityViz opacity={35} /> },
]} />

<OpacityScale />

## Mix Blend Mode

<PropTable rows={[
  { sz: "{ mixBlend: 'multiply' }", tw: "mix-blend-multiply" },
  { sz: "{ mixBlend: 'screen' }", tw: "mix-blend-screen" },
  { sz: "{ mixBlend: 'overlay' }", tw: "mix-blend-overlay" },
  { sz: "{ mixBlend: 'normal' }", tw: "mix-blend-normal" },
  { sz: "{ bgBlend: 'multiply' }", tw: "bg-blend-multiply" },
]} />

## Filter: Blur

<PropTable rows={[
  { sz: "{ blur: 'sm' }", tw: "blur-sm" },
  { sz: "{ blur: true }", tw: "blur" },
  { sz: "{ blur: 'md' }", tw: "blur-md" },
  { sz: "{ blur: 'lg' }", tw: "blur-lg" },
  { sz: "{ blur: 'xl' }", tw: "blur-xl" },
  { sz: "{ blur: '2xl' }", tw: "blur-2xl" },
  { sz: "{ blur: 'none' }", tw: "blur-none" },
  { sz: "{ blur: '10px' }", tw: "blur-[10px]" },
]} />

<BlurScale />

## Filter: Grayscale, Brightness, Contrast

<PropTable rows={[
  { sz: "{ grayscale: true }", tw: "grayscale" },
  { sz: "{ grayscale: 0 }", tw: "grayscale-0" },
  { sz: "{ brightness: 50 }", tw: "brightness-50" },
  { sz: "{ brightness: 110 }", tw: "brightness-110" },
  { sz: "{ contrast: 50 }", tw: "contrast-50" },
  { sz: "{ contrast: 125 }", tw: "contrast-125" },
  { sz: "{ saturate: 0 }", tw: "saturate-0" },
  { sz: "{ saturate: 150 }", tw: "saturate-150" },
  { sz: "{ invert: true }", tw: "invert" },
  { sz: "{ sepia: true }", tw: "sepia" },
]} />

<GrayscaleFilter />

## Filter: Hue Rotate & Drop Shadow

<PropTable rows={[
  { sz: "{ hueRotate: 90 }", tw: "hue-rotate-90" },
  { sz: "{ hueRotate: -30 }", tw: "-hue-rotate-30" },
  { sz: "{ dropShadow: 'sm' }", tw: "drop-shadow-sm" },
  { sz: "{ dropShadow: 'lg' }", tw: "drop-shadow-lg" },
  { sz: "{ dropShadow: 'none' }", tw: "drop-shadow-none" },
  { sz: "{ dropShadow: '0 0 15px rgba(45,213,151,0.5)' }", tw: "drop-shadow-[0_0_15px_rgba(45,213,151,0.5)]" },
  { sz: "{ hover: { dropShadow: '0 0 15px rgba(45,213,151,0.5)' } }", tw: "hover:drop-shadow-[0_0_15px_rgba(45,213,151,0.5)]" },
]} />

## Backdrop Filter

<PropTable rows={[
  { sz: "{ backdropBlur: 'sm' }", tw: "backdrop-blur-sm" },
  { sz: "{ backdropBlur: 'md' }", tw: "backdrop-blur-md" },
  { sz: "{ backdropBrightness: 75 }", tw: "backdrop-brightness-75" },
  { sz: "{ backdropContrast: 125 }", tw: "backdrop-contrast-125" },
  { sz: "{ backdropGrayscale: true }", tw: "backdrop-grayscale" },
  { sz: "{ backdropSaturate: 200 }", tw: "backdrop-saturate-200" },
]} />

## Mask

Tailwind composites `mask-image` from three independent layers, one per CSS
custom property:

```css
mask-image: var(--tw-mask-linear), var(--tw-mask-radial), var(--tw-mask-conic);
```

so each layer is its own sz key. They stack — a linear and a radial mask
declared together compose rather than fight.

:::caution[Breaking: the flat mask stop keys were removed]
The old flat spellings copied the background gradient's bare stops, which
Tailwind never serves for masks (a mask stop is always scoped to its layer or
edge). They now map onto the layer keys:

| Before | After |
| --- | --- |
| `{ mask: 'linear-45' }` | `{ maskLinear: { angle: 45 } }` |
| `{ maskFrom: '20%' }` | `{ maskLinear: { from: '20%' } }` (or `maskRadial` / `maskConic`) |
| `{ maskTo: '80%' }` | `{ maskLinear: { to: '80%' } }` (or `maskRadial` / `maskConic`) |
| `{ maskVia: '50%' }` | Removed with no equivalent — Tailwind has no via stop for masks. |
| `{ maskShape: 'circle' }` | `{ maskRadial: { shape: 'circle' } }` |

`mask` itself now carries only a direct mask-image: `none`, a `url(…)`, a CSS
variable, or an arbitrary value. The compiler warns on every removed spelling
(in the browser dev console too) and names the replacement.
:::

### Linear layer

Declare **either** an angle **or** per-side edges. Both write
`--tw-mask-linear`, so they are alternative modes; declaring both keeps
whichever came later. Sides compose with each other, because each owns its own
variable, and `x` / `y` simply write two.

<PropTable rows={[
  { sz: "{ maskLinear: { angle: 45 } }", tw: "mask-linear-45" },
  { sz: "{ maskLinear: { angle: -45 } }", tw: "-mask-linear-45" },
  { sz: "{ maskLinear: { angle: 45, from: '20%', to: '80%' } }", tw: "mask-linear-45 mask-linear-from-20% mask-linear-to-80%" },
  { sz: "{ maskLinear: { b: { from: '20%', to: '80%' } } }", tw: "mask-b-from-20% mask-b-to-80%" },
  { sz: "{ maskLinear: { t: { from: '0%' }, b: { from: '60%' } } }", tw: "mask-t-from-0% mask-b-from-60%" },
  { sz: "{ maskLinear: { x: { from: '20%' } } }", tw: "mask-x-from-20%" },
]} />

### Radial and conic layers

`at`, `size` and `shape` each write their own variable, so they compose with the
stops and with each other.

<PropTable rows={[
  { sz: "{ maskRadial: { at: 'top', shape: 'circle' } }", tw: "mask-radial-at-top mask-circle" },
  { sz: "{ maskRadial: { size: 'closest-side', from: '0%', to: '100%' } }", tw: "mask-radial-closest-side mask-radial-from-0% mask-radial-to-100%" },
  { sz: "{ maskConic: { angle: 90 } }", tw: "mask-conic-90" },
  { sz: "{ maskConic: { from: '20%', to: '80%' } }", tw: "mask-conic-from-20% mask-conic-to-80%" },
]} />

### Gradient stops

A stop carries a **position**, a **colour**, or both — Tailwind keeps them in
different custom properties, so a stop with both emits one utility for each. A
scalar is a position when numeric and a colour otherwise; a bare CSS variable
reads as a position, so a variable meant as a colour goes in `color`.

<PropTable rows={[
  { sz: "{ maskLinear: { b: { from: '20%' } } }", tw: "mask-b-from-20%" },
  { sz: "{ maskLinear: { b: { from: 'red-500' } } }", tw: "mask-b-from-red-500" },
  { sz: "{ maskLinear: { b: { from: { color: 'red-500', op: 30 } } } }", tw: "mask-b-from-red-500/30" },
  { sz: "{ maskLinear: { b: { from: { at: '20%', color: 'red-500' } } } }", tw: "mask-b-from-20% mask-b-from-red-500" },
  { sz: "{ maskLinear: { b: { from: { at: '--c' } } } }", tw: "mask-b-from-(--c)" },
  { sz: "{ maskLinear: { b: { from: { color: '--c' } } } }", tw: "mask-b-from-(color:--c)" },
]} />

### The rest of the mask family

`mask` itself now carries a direct `mask-image` only — a gradient value belongs
to its layer key.

<PropTable rows={[
  { sz: "{ mask: 'none' }", tw: "mask-none" },
  { sz: "{ mask: \"url('/img.png')\" }", tw: "mask-[url('/img.png')]" },
  { sz: "{ mask: '--my-mask' }", tw: "mask-(--my-mask)" },
  { sz: "{ maskSize: 'cover' }", tw: "mask-cover" },
  { sz: "{ maskSize: '50%' }", tw: "mask-size-[50%]" },
  { sz: "{ maskPos: 'top-left' }", tw: "mask-top-left" },
  { sz: "{ maskRepeat: 'space' }", tw: "mask-repeat-space" },
  { sz: "{ maskClip: 'no-clip' }", tw: "mask-no-clip" },
  { sz: "{ maskMode: 'match-source' }", tw: "mask-match" },
  { sz: "{ maskType: 'alpha' }", tw: "mask-type-alpha" },
  { sz: "{ maskComposite: 'add' }", tw: "mask-add" },
]} />

## Examples

```tsx
// Card with elevation shadow
<div sz={{
  shadow: 'lg',
  rounded: 'xl',
  p: 6,
  bg: 'white',
}} />

// Faded disabled state
<button sz={{
  opacity: 50,
  cursor: 'not-allowed',
  disabled: { opacity: 50 },
}} />

// Frosted glass panel
<div sz={{
  backdropBlur: 'md',
  bg: { color: 'white', op: 80 },
  border: 1,
  borderColor: { color: 'white', op: 40 },
}} />

// Blurred background image
<div sz={{
  position: 'relative',
}}>
  <img sz={{
    position: 'absolute',
    inset: 0,
    w: 'full',
    h: 'full',
    objectFit: 'cover',
    blur: 'xl',
  }} />
</div>
```
